# Salin file ini jadi ".env" lalu isi dengan kredensial asli.
# JANGAN commit file ".env" ke git / kirim ke siapa pun.

# --- Email pengirim (Gmail SMTP + App Password, bukan password biasa) ---
# Cara buat App Password: Google Account -> Security -> App Passwords
SENDER_EMAIL=your-email@gmail.com
EMAIL_APP_PASSWORD=your16charapppassword

# --- Daftar penerima laporan, pisahkan dengan koma ---
RECEIVER_EMAILS=penerima1@example.com,penerima2@example.com

# --- (Opsional) API key berita dari https://newsapi.org ---
# Kosongkan jika tidak dipakai; bot akan otomatis melewati fitur berita.
NEWS_API_KEY=
